const Comment = require("../models/comment");

const postComment = async (req, res, next) => {
    
};

const deleteComment;

const readComments;

const readSingleComment;

module.exports = { deleteComment, readComments, readSingleComment, postComment}
